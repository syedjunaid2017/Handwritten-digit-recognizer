# Handwritten-digit-recognizer
This project implements a Handwritten Digit Recognizer using a Convolutional Neural Network (CNN) trained on the MNIST dataset. The goal of the project is to demonstrate how deep learning techniques can be applied to image classification tasks. The model is trained on 70,000 grayscale images of handwritten digits (0–9), each of size 28×28 pixels. 
